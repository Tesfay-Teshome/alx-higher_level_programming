-- Creates a table called first_table in the current database in your MySQL server
-- Query to create a table in current database
CREATE TABLE IF NOT EXISTS first_table (
       id INT,
       name VARCHAR(256)
);